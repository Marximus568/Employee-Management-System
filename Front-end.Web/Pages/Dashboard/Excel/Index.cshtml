@page
@model Front_end.Web.Pages.Dashboard.Excel.IndexModel
@{
    ViewData["Title"] = "Excel Reports";
    Layout = "_DashboardLayout";
}

<div class="mb-4">
    <h1>Excel Management</h1>
    <p style="color: var(--color-gray-500);">Export data to Excel or import records in bulk.</p>
</div>

<div class="grid grid-2">

    <!-- Bulk Import Card -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">
                <i class="fas fa-file-import" style="color: var(--color-primary); margin-right: 10px;"></i>
                Bulk Import
            </h3>
        </div>
        <div class="card-body">
            @if (!string.IsNullOrEmpty(Model.Message))
            {
                <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" style="padding: 10px; border-radius: 5px; margin-bottom: 15px; background-color: @(Model.IsSuccess ? "#dcfce7" : "#fee2e2"); color: @(Model.IsSuccess ? "#166534" : "#991b1b");">
                    @Model.Message
                </div>
            }

            <p>Import multiple records at once using a standard Excel template.</p>
            
            <form method="post" enctype="multipart/form-data">
                <!-- Import Type Selector Removed -->
                <div class="form-group">
                    <label class="form-label">Upload File</label>
                    <input type="file" asp-for="Upload" class="form-control" accept=".xlsx" style="padding: 10px; height: auto;" />
                    <p style="margin-top: 5px; font-size: 0.8rem; color: var(--color-gray-400);">.xlsx files only</p>
                </div>
                <button type="submit" asp-page-handler="Import" class="btn btn-primary" style="width: 100%;">
                    <i class="fas fa-upload"></i> Importar Excel
                </button>
            </form>
        </div>
    </div>
</div>
